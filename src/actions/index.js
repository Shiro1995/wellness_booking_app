import { createAction } from 'redux-actions';

export const onLogin = createAction('onLogin');
export const onGetBooking = createAction('onGetBooking');
export const onCreateBooking = createAction('onCreateBooking');
